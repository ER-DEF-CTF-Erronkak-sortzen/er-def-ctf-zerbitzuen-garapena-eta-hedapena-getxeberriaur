version: '3'
services:
    php:
        build:
            context: ./service/php
            dockerfile: Dockerfile
        ports:
            - "8001:80"
        volumes:
            - ./service/php:/var/www/html
                        